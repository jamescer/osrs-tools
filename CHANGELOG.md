# osrs-tools

This project uses [changesets](https://github.com/changesets/changesets) to manage releases. Entries below this point are generated automatically by `npm run changeset:version` from the changeset files contributors add in `.changeset/` — see `.changeset/README.md` for how to add one.

Versions prior to `2.9.3` predate this file; see the [git history](https://github.com/jamescer/osrs-tools/commits/master) for changes made before it was introduced.
