import { Item } from "../Item";

const AlchemicalChartInterfaceItemData = {
  id: interface8354,
  name: "Alchemical chart (interface item)"
};
export const AlchemicalChartInterfaceItem = Item.fromJson(AlchemicalChartInterfaceItemData);