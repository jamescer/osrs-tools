import { Item } from "../Item";

const ArcheryTargetInterfaceItemData = {
  id: interface8164,
  name: "Archery target (interface item)"
};
export const ArcheryTargetInterfaceItem = Item.fromJson(ArcheryTargetInterfaceItemData);