import { Item } from "../Item";

const BalanceBeamInterfaceItemData = {
  id: interface8027,
  name: "Balance beam (interface item)"
};
export const BalanceBeamInterfaceItem = Item.fromJson(BalanceBeamInterfaceItemData);
