import { Item } from "../Item";

const BannerEaselInterfaceItemData = {
  id: interface8394,
  name: "Banner easel (interface item)"
};
export const BannerEaselInterfaceItem = Item.fromJson(BannerEaselInterfaceItemData);