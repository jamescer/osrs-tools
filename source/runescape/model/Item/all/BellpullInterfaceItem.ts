import { Item } from "../Item";

const BellpullInterfaceItemData = {
  id: interface8100,
  name: "Bell-pull (interface item)"
};
export const BellpullInterfaceItem = Item.fromJson(BellpullInterfaceItemData);
