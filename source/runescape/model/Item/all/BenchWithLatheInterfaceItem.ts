import { Item } from "../Item";

const BenchWithLatheInterfaceItemData = {
  id: interface8379,
  name: "Bench with lathe (interface item)"
};
export const BenchWithLatheInterfaceItem = Item.fromJson(BenchWithLatheInterfaceItemData);