import { Item } from "../Item";

const BenchWithViceInterfaceItemData = {
  id: interface8378,
  name: "Bench with vice (interface item)"
};
export const BenchWithViceInterfaceItem = Item.fromJson(BenchWithViceInterfaceItemData);