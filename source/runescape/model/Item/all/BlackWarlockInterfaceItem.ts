import { Item } from "../Item";

const BlackWarlockInterfaceItemData = {
  id: interface9973,
  name: "Black warlock (interface item)"
};
export const BlackWarlockInterfaceItem = Item.fromJson(BlackWarlockInterfaceItemData);