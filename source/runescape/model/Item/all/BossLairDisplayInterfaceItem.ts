import { Item } from "../Item";

const BossLairDisplayInterfaceItemData = {
  id: interface20629,
  name: "Boss lair display (interface item)"
};
export const BossLairDisplayInterfaceItem = Item.fromJson(BossLairDisplayInterfaceItemData);
