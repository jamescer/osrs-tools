import { Item } from "../Item";

const BoundaryStonesInterfaceItemData = {
  id: interface8196,
  name: "Boundary stones (interface item)"
};
export const BoundaryStonesInterfaceItem = Item.fromJson(BoundaryStonesInterfaceItemData);
