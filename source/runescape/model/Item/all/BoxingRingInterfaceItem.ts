import { Item } from "../Item";

const BoxingRingInterfaceItemData = {
  id: interface8023,
  name: "Boxing ring (interface item)"
};
export const BoxingRingInterfaceItem = Item.fromJson(BoxingRingInterfaceItemData);