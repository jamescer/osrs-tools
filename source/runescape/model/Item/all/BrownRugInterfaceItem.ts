import { Item } from "../Item";

const BrownRugInterfaceItemData = {
  id: interface8316,
  name: "Brown rug (interface item)"
};
export const BrownRugInterfaceItem = Item.fromJson(BrownRugInterfaceItemData);