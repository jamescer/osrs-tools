import { Item } from "../Item";

const CandlesInterfaceItemData = {
  id: interface8128,
  name: "Candles (interface item)"
};
export const CandlesInterfaceItem = Item.fromJson(CandlesInterfaceItemData);