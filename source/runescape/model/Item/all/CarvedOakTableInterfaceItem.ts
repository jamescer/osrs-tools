import { Item } from "../Item";

const CarvedOakTableInterfaceItemData = {
  id: interface8117,
  name: "Carved oak table (interface item)"
};
export const CarvedOakTableInterfaceItem = Item.fromJson(CarvedOakTableInterfaceItemData);
