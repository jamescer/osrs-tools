import { Item } from "../Item";

const CarvedTeakBenchInterfaceItemData = {
  id: interface8112,
  name: "Carved teak bench (interface item)"
};
export const CarvedTeakBenchInterfaceItem = Item.fromJson(CarvedTeakBenchInterfaceItemData);