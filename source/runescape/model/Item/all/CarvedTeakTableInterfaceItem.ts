import { Item } from "../Item";

const CarvedTeakTableInterfaceItemData = {
  id: interface8119,
  name: "Carved teak table (interface item)"
};
export const CarvedTeakTableInterfaceItem = Item.fromJson(CarvedTeakTableInterfaceItemData);