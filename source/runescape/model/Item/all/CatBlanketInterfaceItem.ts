import { Item } from "../Item";

const CatBlanketInterfaceItemData = {
  id: interface8236,
  name: "Cat blanket (interface item)"
};
export const CatBlanketInterfaceItem = Item.fromJson(CatBlanketInterfaceItemData);
