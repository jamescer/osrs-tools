import { Item } from "../Item";

const ChefsDelightBarrelInterfaceItemData = {
  id: interface8244,
  name: "Chef's delight (barrel) (interface item)"
};
export const ChefsDelightBarrelInterfaceItem = Item.fromJson(ChefsDelightBarrelInterfaceItemData);