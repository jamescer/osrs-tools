import { Item } from "../Item";

const ChinchompaInterfaceItemData = {
  id: interface9976,
  name: "Chinchompa (interface item)"
};
export const ChinchompaInterfaceItem = Item.fromJson(ChinchompaInterfaceItemData);
