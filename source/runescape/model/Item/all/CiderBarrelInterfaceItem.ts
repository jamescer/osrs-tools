import { Item } from "../Item";

const CiderBarrelInterfaceItemData = {
  id: interface8240,
  name: "Cider barrel (interface item)"
};
export const CiderBarrelInterfaceItem = Item.fromJson(CiderBarrelInterfaceItemData);
