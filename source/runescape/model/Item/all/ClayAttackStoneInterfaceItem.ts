import { Item } from "../Item";

const ClayAttackStoneInterfaceItemData = {
  id: interface8153,
  name: "Clay attack stone (interface item)"
};
export const ClayAttackStoneInterfaceItem = Item.fromJson(ClayAttackStoneInterfaceItemData);
