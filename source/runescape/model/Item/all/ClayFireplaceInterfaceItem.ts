import { Item } from "../Item";

const ClayFireplaceInterfaceItemData = {
  id: interface8325,
  name: "Clay fireplace (interface item)"
};
export const ClayFireplaceInterfaceItem = Item.fromJson(ClayFireplaceInterfaceItemData);
