import { Item } from "../Item";

const CockatriceInterfaceItemData = {
  id: interface4137,
  name: "Cockatrice (interface item)"
};
export const CockatriceInterfaceItem = Item.fromJson(CockatriceInterfaceItemData);