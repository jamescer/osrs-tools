import { Item } from "../Item";

const CombatRingInterfaceItemData = {
  id: interface8025,
  name: "Combat ring (interface item)"
};
export const CombatRingInterfaceItem = Item.fromJson(CombatRingInterfaceItemData);