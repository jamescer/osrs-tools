import { Item } from "../Item";

const ConsecratedHouseInterfaceItemData = {
  id: interface12707,
  name: "Consecrated house (interface item)"
};
export const ConsecratedHouseInterfaceItem = Item.fromJson(ConsecratedHouseInterfaceItemData);
