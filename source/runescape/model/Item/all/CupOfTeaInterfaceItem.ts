import { Item } from "../Item";

const CupOfTeaInterfaceItemData = {
  id: interface1978,
  name: "Cup of tea (interface item)"
};
export const CupOfTeaInterfaceItem = Item.fromJson(CupOfTeaInterfaceItemData);
