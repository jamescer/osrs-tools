import { Item } from "../Item";

const CurtainsInterfaceItemData = {
  id: interface8323,
  name: "Curtains (interface item)"
};
export const CurtainsInterfaceItem = Item.fromJson(CurtainsInterfaceItemData);
