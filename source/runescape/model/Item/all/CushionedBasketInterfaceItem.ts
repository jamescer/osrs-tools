import { Item } from "../Item";

const CushionedBasketInterfaceItemData = {
  id: interface8238,
  name: "Cushioned basket (interface item)"
};
export const CushionedBasketInterfaceItem = Item.fromJson(CushionedBasketInterfaceItemData);