import { Item } from "../Item";

const DagannothInterfaceItemData = {
  id: interface8141,
  name: "Dagannoth (interface item)"
};
export const DagannothInterfaceItem = Item.fromJson(DagannothInterfaceItemData);
