import { Item } from "../Item";

const DartboardInterfaceItemData = {
  id: interface8163,
  name: "Dartboard (interface item)"
};
export const DartboardInterfaceItem = Item.fromJson(DartboardInterfaceItemData);
