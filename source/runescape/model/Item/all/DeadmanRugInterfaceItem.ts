import { Item } from "../Item";

const DeadmanRugInterfaceItemData = {
  id: interface29598,
  name: "Deadman rug (interface item)"
};
export const DeadmanRugInterfaceItem = Item.fromJson(DeadmanRugInterfaceItemData);
