import { Item } from "../Item";

const DecorativePipeInterfaceItemData = {
  id: interface8126,
  name: "Decorative pipe (interface item)"
};
export const DecorativePipeInterfaceItem = Item.fromJson(DecorativePipeInterfaceItemData);