import { Item } from "../Item";

const DecorativeRockInterfaceItemData = {
  id: interface8169,
  name: "Decorative rock (interface item)"
};
export const DecorativeRockInterfaceItem = Item.fromJson(DecorativeRockInterfaceItemData);
