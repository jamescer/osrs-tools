import { Item } from "../Item";

const DecorativeWindowInterfaceItemData = {
  id: interface8077,
  name: "Decorative window (interface item)"
};
export const DecorativeWindowInterfaceItem = Item.fromJson(DecorativeWindowInterfaceItemData);