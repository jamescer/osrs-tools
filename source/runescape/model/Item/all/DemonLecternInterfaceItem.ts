import { Item } from "../Item";

const DemonLecternInterfaceItemData = {
  id: interface8336,
  name: "Demon lectern (interface item)"
};
export const DemonLecternInterfaceItem = Item.fromJson(DemonLecternInterfaceItemData);