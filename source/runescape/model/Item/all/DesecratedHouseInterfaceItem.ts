import { Item } from "../Item";

const DesecratedHouseInterfaceItemData = {
  id: interface12708,
  name: "Desecrated house (interface item)"
};
export const DesecratedHouseInterfaceItem = Item.fromJson(DesecratedHouseInterfaceItemData);
