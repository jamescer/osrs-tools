import { Item } from "../Item";

const DesertHabitatInterfaceItemData = {
  id: interface12712,
  name: "Desert habitat (interface item)"
};
export const DesertHabitatInterfaceItem = Item.fromJson(DesertHabitatInterfaceItemData);
