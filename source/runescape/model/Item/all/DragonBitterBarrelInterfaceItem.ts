import { Item } from "../Item";

const DragonBitterBarrelInterfaceItemData = {
  id: interface8243,
  name: "Dragon bitter (barrel) (interface item)"
};
export const DragonBitterBarrelInterfaceItem = Item.fromJson(DragonBitterBarrelInterfaceItemData);
