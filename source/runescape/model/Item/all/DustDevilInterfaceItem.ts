import { Item } from "../Item";

const DustDevilInterfaceItemData = {
  id: interface4145,
  name: "Dust Devil (interface item)"
};
export const DustDevilInterfaceItem = Item.fromJson(DustDevilInterfaceItemData);
