import { Item } from "../Item";

const EliteHideyHoleInterfaceItemData = {
  id: interface20535,
  name: "Elite hidey hole (interface item)"
};
export const EliteHideyHoleInterfaceItem = Item.fromJson(EliteHideyHoleInterfaceItemData);
