import { Item } from "../Item";

const ExitPortalInterfaceItemData = {
  id: interface8168,
  name: "Exit portal (interface item)"
};
export const ExitPortalInterfaceItem = Item.fromJson(ExitPortalInterfaceItemData);
