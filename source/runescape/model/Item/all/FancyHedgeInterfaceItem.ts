import { Item } from "../Item";

const FancyHedgeInterfaceItemData = {
  id: interface8207,
  name: "Fancy hedge (interface item)"
};
export const FancyHedgeInterfaceItem = Item.fromJson(FancyHedgeInterfaceItemData);
