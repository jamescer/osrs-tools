import { Item } from "../Item";

const FarmerInterfaceItemData = {
  id: interface3243,
  name: "Farmer (interface item)"
};
export const FarmerInterfaceItem = Item.fromJson(FarmerInterfaceItemData);
