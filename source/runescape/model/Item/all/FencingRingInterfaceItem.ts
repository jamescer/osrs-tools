import { Item } from "../Item";

const FencingRingInterfaceItemData = {
  id: interface8024,
  name: "Fencing ring (interface item)"
};
export const FencingRingInterfaceItem = Item.fromJson(FencingRingInterfaceItemData);
