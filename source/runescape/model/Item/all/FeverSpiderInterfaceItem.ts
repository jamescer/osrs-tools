import { Item } from "../Item";

const FeverSpiderInterfaceItemData = {
  id: interface6709,
  name: "Fever spider (interface item)"
};
export const FeverSpiderInterfaceItem = Item.fromJson(FeverSpiderInterfaceItemData);
