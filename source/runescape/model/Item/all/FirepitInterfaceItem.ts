import { Item } from "../Item";

const FirepitInterfaceItemData = {
  id: interface8216,
  name: "Firepit (interface item)"
};
export const FirepitInterfaceItem = Item.fromJson(FirepitInterfaceItemData);