import { Item } from "../Item";

const FirepitWithHookInterfaceItemData = {
  id: interface8217,
  name: "Firepit with hook (interface item)"
};
export const FirepitWithHookInterfaceItem = Item.fromJson(FirepitWithHookInterfaceItemData);
