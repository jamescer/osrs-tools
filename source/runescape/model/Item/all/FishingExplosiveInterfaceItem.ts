import { Item } from "../Item";

const FishingExplosiveInterfaceItemData = {
  id: interface6660,
  name: "Fishing explosive (interface item)"
};
export const FishingExplosiveInterfaceItem = Item.fromJson(FishingExplosiveInterfaceItemData);