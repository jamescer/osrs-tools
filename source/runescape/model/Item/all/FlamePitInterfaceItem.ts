import { Item } from "../Item";

const FlamePitInterfaceItemData = {
  id: interface8304,
  name: "Flame pit (interface item)"
};
export const FlamePitInterfaceItem = Item.fromJson(FlamePitInterfaceItemData);