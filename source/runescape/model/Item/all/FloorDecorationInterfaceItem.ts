import { Item } from "../Item";

const FloorDecorationInterfaceItemData = {
  id: interface8370,
  name: "Floor decoration (interface item)"
};
export const FloorDecorationInterfaceItem = Item.fromJson(FloorDecorationInterfaceItemData);