import { Item } from "../Item";

const ForestHabitatInterfaceItemData = {
  id: interface12711,
  name: "Forest habitat (interface item)"
};
export const ForestHabitatInterfaceItem = Item.fromJson(ForestHabitatInterfaceItemData);
