import { Item } from "../Item";

const FremennikInterfaceItemData = {
  id: interface3686,
  name: "Fremennik (interface item)"
};
export const FremennikInterfaceItem = Item.fromJson(FremennikInterfaceItemData);