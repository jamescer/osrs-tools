import { Item } from "../Item";

const GardenFenceInterfaceItemData = {
  id: interface8201,
  name: "Garden fence (interface item)"
};
export const GardenFenceInterfaceItem = Item.fromJson(GardenFenceInterfaceItemData);