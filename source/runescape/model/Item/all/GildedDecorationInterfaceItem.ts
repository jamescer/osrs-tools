import { Item } from "../Item";

const GildedDecorationInterfaceItemData = {
  id: interface8104,
  name: "Gilded decoration (interface item)"
};
export const GildedDecorationInterfaceItem = Item.fromJson(GildedDecorationInterfaceItemData);
