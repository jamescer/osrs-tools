import { Item } from "../Item";

const GildedPortalNexusInterfaceItemData = {
  id: interface22706,
  name: "Gilded portal nexus (interface item)"
};
export const GildedPortalNexusInterfaceItem = Item.fromJson(GildedPortalNexusInterfaceItemData);
