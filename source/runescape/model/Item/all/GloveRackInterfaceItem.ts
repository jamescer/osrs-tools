import { Item } from "../Item";

const GloveRackInterfaceItemData = {
  id: interface8028,
  name: "Glove rack (interface item)"
};
export const GloveRackInterfaceItem = Item.fromJson(GloveRackInterfaceItemData);
