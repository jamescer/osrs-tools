import { Item } from "../Item";

const GnomeBenchInterfaceItemData = {
  id: interface20650,
  name: "Gnome bench (interface item)"
};
export const GnomeBenchInterfaceItem = Item.fromJson(GnomeBenchInterfaceItemData);
