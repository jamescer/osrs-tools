import { Item } from "../Item";

const GoldSatchelInterfaceItemData = {
  id: interface25622,
  name: "Gold satchel (interface item)"
};
export const GoldSatchelInterfaceItem = Item.fromJson(GoldSatchelInterfaceItemData);
