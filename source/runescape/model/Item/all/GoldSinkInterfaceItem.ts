import { Item } from "../Item";

const GoldSinkInterfaceItemData = {
  id: interface26269,
  name: "Gold sink (interface item)"
};
export const GoldSinkInterfaceItem = Item.fromJson(GoldSinkInterfaceItemData);
