import { Item } from "../Item";

const GrasslandHabitatInterfaceItemData = {
  id: interface12710,
  name: "Grassland habitat (interface item)"
};
export const GrasslandHabitatInterfaceItem = Item.fromJson(GrasslandHabitatInterfaceItemData);
