import { Item } from "../Item";

const GreaterMagicalBalanceInterfaceItemData = {
  id: interface8158,
  name: "Greater magical balance (interface item)"
};
export const GreaterMagicalBalanceInterfaceItem = Item.fromJson(GreaterMagicalBalanceInterfaceItemData);
