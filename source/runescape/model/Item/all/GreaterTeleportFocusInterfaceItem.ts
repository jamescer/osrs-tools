import { Item } from "../Item";

const GreaterTeleportFocusInterfaceItemData = {
  id: interface8332,
  name: "Greater teleport focus (interface item)"
};
export const GreaterTeleportFocusInterfaceItem = Item.fromJson(GreaterTeleportFocusInterfaceItemData);
