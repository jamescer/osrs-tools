import { Item } from "../Item";

const GreenSatchelInterfaceItemData = {
  id: interface25619,
  name: "Green satchel (interface item)"
};
export const GreenSatchelInterfaceItem = Item.fromJson(GreenSatchelInterfaceItemData);