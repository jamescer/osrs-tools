import { Item } from "../Item";

const GuardDogInterfaceItemData = {
  id: interface8132,
  name: "Guard dog (interface item)"
};
export const GuardDogInterfaceItem = Item.fromJson(GuardDogInterfaceItemData);
