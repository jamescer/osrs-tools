import { Item } from "../Item";

const HangingSkeletonInterfaceItemData = {
  id: interface8127,
  name: "Hanging skeleton (interface item)"
};
export const HangingSkeletonInterfaceItem = Item.fromJson(HangingSkeletonInterfaceItemData);