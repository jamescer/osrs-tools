import { Item } from "../Item";

const HangmanInterfaceItemData = {
  id: interface8161,
  name: "Hangman (interface item)"
};
export const HangmanInterfaceItem = Item.fromJson(HangmanInterfaceItemData);