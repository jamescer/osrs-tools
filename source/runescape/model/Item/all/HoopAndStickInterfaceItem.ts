import { Item } from "../Item";

const HoopAndStickInterfaceItemData = {
  id: interface8162,
  name: "Hoop and stick (interface item)"
};
export const HoopAndStickInterfaceItem = Item.fromJson(HoopAndStickInterfaceItemData);