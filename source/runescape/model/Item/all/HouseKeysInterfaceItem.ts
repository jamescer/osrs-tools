import { Item } from "../Item";

const HouseKeysInterfaceItemData = {
  id: interface29328,
  name: "House keys (interface item)"
};
export const HouseKeysInterfaceItem = Item.fromJson(HouseKeysInterfaceItemData);
