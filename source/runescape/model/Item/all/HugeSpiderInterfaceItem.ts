import { Item } from "../Item";

const HugeSpiderInterfaceItemData = {
  id: interface8135,
  name: "Huge spider (interface item)"
};
export const HugeSpiderInterfaceItem = Item.fromJson(HugeSpiderInterfaceItemData);
