import { Item } from "../Item";

const ImpStatueInterfaceItemData = {
  id: interface8171,
  name: "Imp statue (interface item)"
};
export const ImpStatueInterfaceItem = Item.fromJson(ImpStatueInterfaceItemData);
