import { Item } from "../Item";

const InfernalChartInterfaceItemData = {
  id: interface8356,
  name: "Infernal chart (interface item)"
};
export const InfernalChartInterfaceItem = Item.fromJson(InfernalChartInterfaceItemData);
