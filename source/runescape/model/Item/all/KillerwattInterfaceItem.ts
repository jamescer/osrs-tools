import { Item } from "../Item";

const KillerwattInterfaceItemData = {
  id: interface7160,
  name: "Killerwatt (interface item)"
};
export const KillerwattInterfaceItem = Item.fromJson(KillerwattInterfaceItemData);
