import { Item } from "../Item";

const KiteShieldInterfaceItemData = {
  id: interface8107,
  name: "Kite shield (interface item)"
};
export const KiteShieldInterfaceItem = Item.fromJson(KiteShieldInterfaceItemData);