import { Item } from "../Item";

const KnightInterfaceItemData = {
  id: interface3251,
  name: "Knight (interface item)"
};
export const KnightInterfaceItem = Item.fromJson(KnightInterfaceItemData);
