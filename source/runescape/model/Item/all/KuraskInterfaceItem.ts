import { Item } from "../Item";

const KuraskInterfaceItemData = {
  id: interface4146,
  name: "Kurask (interface item)"
};
export const KuraskInterfaceItem = Item.fromJson(KuraskInterfaceItemData);
