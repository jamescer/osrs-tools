import { Item } from "../Item";

const LargeLandscapeInterfaceItemData = {
  id: interface8094,
  name: "Large landscape (interface item)"
};
export const LargeLandscapeInterfaceItem = Item.fromJson(LargeLandscapeInterfaceItemData);