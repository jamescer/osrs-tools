import { Item } from "../Item";

const LargeOvenInterfaceItemData = {
  id: interface8220,
  name: "Large oven (interface item)"
};
export const LargeOvenInterfaceItem = Item.fromJson(LargeOvenInterfaceItemData);
