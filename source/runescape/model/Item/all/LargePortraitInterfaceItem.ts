import { Item } from "../Item";

const LargePortraitInterfaceItemData = {
  id: interface8093,
  name: "Large portrait (interface item)"
};
export const LargePortraitInterfaceItem = Item.fromJson(LargePortraitInterfaceItemData);
