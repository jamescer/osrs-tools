import { Item } from "../Item";

const LargeTeakBedInterfaceItemData = {
  id: interface8035,
  name: "Large teak bed (interface item)"
};
export const LargeTeakBedInterfaceItem = Item.fromJson(LargeTeakBedInterfaceItemData);