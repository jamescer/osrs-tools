import { Item } from "../Item";

const LeagueAccomplishmentsScrollInterfaceItemData = {
  id: interface25365,
  name: "League accomplishments scroll (interface item)"
};
export const LeagueAccomplishmentsScrollInterfaceItem = Item.fromJson(LeagueAccomplishmentsScrollInterfaceItemData);