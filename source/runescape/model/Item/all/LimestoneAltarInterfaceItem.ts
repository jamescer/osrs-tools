import { Item } from "../Item";

const LimestoneAltarInterfaceItemData = {
  id: interface8066,
  name: "Limestone altar (interface item)"
};
export const LimestoneAltarInterfaceItem = Item.fromJson(LimestoneAltarInterfaceItemData);
