import { Item } from "../Item";

const LowlevelPlantInterfaceItemData = {
  id: interface8096,
  name: "Low-level plant (interface item)"
};
export const LowlevelPlantInterfaceItem = Item.fromJson(LowlevelPlantInterfaceItemData);
