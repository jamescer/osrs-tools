import { Item } from "../Item";

const LunarAltarInterfaceItemData = {
  id: interface20618,
  name: "Lunar altar (interface item)"
};
export const LunarAltarInterfaceItem = Item.fromJson(LunarAltarInterfaceItemData);