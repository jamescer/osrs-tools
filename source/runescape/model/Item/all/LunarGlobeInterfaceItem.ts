import { Item } from "../Item";

const LunarGlobeInterfaceItemData = {
  id: interface8343,
  name: "Lunar globe (interface item)"
};
export const LunarGlobeInterfaceItem = Item.fromJson(LunarGlobeInterfaceItemData);
