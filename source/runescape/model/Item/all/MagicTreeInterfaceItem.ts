import { Item } from "../Item";

const MagicTreeInterfaceItemData = {
  id: interface8179,
  name: "Magic tree (interface item)"
};
export const MagicTreeInterfaceItem = Item.fromJson(MagicTreeInterfaceItemData);