import { Item } from "../Item";

const MahoganyPortalInterfaceItemData = {
  id: interface8329,
  name: "Mahogany portal (interface item)"
};
export const MahoganyPortalInterfaceItem = Item.fromJson(MahoganyPortalInterfaceItemData);