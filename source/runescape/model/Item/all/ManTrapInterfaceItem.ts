import { Item } from "../Item";

const ManTrapInterfaceItemData = {
  id: interface8144,
  name: "Man trap (interface item)"
};
export const ManTrapInterfaceItem = Item.fromJson(ManTrapInterfaceItemData);