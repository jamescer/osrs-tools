import { Item } from "../Item";

const MarbleAdventureLogInterfaceItemData = {
  id: interface20625,
  name: "Marble adventure log (interface item)"
};
export const MarbleAdventureLogInterfaceItem = Item.fromJson(MarbleAdventureLogInterfaceItemData);
