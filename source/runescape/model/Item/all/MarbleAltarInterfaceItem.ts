import { Item } from "../Item";

const MarbleAltarInterfaceItemData = {
  id: interface8067,
  name: "Marble altar (interface item)"
};
export const MarbleAltarInterfaceItem = Item.fromJson(MarbleAltarInterfaceItemData);
