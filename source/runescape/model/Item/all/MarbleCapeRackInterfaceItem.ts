import { Item } from "../Item";

const MarbleCapeRackInterfaceItemData = {
  id: interface9821,
  name: "Marble cape rack (interface item)"
};
export const MarbleCapeRackInterfaceItem = Item.fromJson(MarbleCapeRackInterfaceItemData);
