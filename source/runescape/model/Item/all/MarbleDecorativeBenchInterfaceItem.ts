import { Item } from "../Item";

const MarbleDecorativeBenchInterfaceItemData = {
  id: interface20651,
  name: "Marble decorative bench (interface item)"
};
export const MarbleDecorativeBenchInterfaceItem = Item.fromJson(MarbleDecorativeBenchInterfaceItemData);
