import { Item } from "../Item";

const MarbleDoorInterfaceItemData = {
  id: interface8124,
  name: "Marble door (interface item)"
};
export const MarbleDoorInterfaceItem = Item.fromJson(MarbleDoorInterfaceItemData);