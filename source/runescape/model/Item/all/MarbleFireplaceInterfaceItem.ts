import { Item } from "../Item";

const MarbleFireplaceInterfaceItemData = {
  id: interface8327,
  name: "Marble fireplace (interface item)"
};
export const MarbleFireplaceInterfaceItem = Item.fromJson(MarbleFireplaceInterfaceItemData);