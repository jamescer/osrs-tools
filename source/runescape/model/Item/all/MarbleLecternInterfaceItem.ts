import { Item } from "../Item";

const MarbleLecternInterfaceItemData = {
  id: interface24132,
  name: "Marble lectern (interface item)"
};
export const MarbleLecternInterfaceItem = Item.fromJson(MarbleLecternInterfaceItemData);