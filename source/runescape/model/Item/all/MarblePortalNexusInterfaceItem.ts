import { Item } from "../Item";

const MarblePortalNexusInterfaceItemData = {
  id: interface22705,
  name: "Marble portal nexus (interface item)"
};
export const MarblePortalNexusInterfaceItem = Item.fromJson(MarblePortalNexusInterfaceItemData);
