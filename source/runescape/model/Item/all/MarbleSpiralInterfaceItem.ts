import { Item } from "../Item";

const MarbleSpiralInterfaceItemData = {
  id: interface8259,
  name: "Marble spiral (interface item)"
};
export const MarbleSpiralInterfaceItem = Item.fromJson(MarbleSpiralInterfaceItemData);
