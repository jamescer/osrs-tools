import { Item } from "../Item";

const MarbleTrapInterfaceItemData = {
  id: interface8146,
  name: "Marble trap (interface item)"
};
export const MarbleTrapInterfaceItem = Item.fromJson(MarbleTrapInterfaceItemData);