import { Item } from "../Item";

const MarigoldsInterfaceItemData = {
  id: interface8214,
  name: "Marigolds (interface item)"
};
export const MarigoldsInterfaceItem = Item.fromJson(MarigoldsInterfaceItemData);