import { Item } from "../Item";

const MasterFarmerInterfaceItemData = {
  id: interface5068,
  name: "Master farmer (interface item)"
};
export const MasterFarmerInterfaceItem = Item.fromJson(MasterFarmerInterfaceItemData);