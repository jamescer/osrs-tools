import { Item } from "../Item";

const MasterHideyHoleInterfaceItemData = {
  id: interface20536,
  name: "Master hidey hole (interface item)"
};
export const MasterHideyHoleInterfaceItem = Item.fromJson(MasterHideyHoleInterfaceItemData);
