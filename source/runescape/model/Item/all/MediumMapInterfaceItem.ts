import { Item } from "../Item";

const MediumMapInterfaceItemData = {
  id: interface8295,
  name: "Medium map (interface item)"
};
export const MediumMapInterfaceItem = Item.fromJson(MediumMapInterfaceItemData);
