import { Item } from "../Item";

const MenaphiteThugInterfaceItemData = {
  id: interface6780,
  name: "Menaphite thug (interface item)"
};
export const MenaphiteThugInterfaceItem = Item.fromJson(MenaphiteThugInterfaceItemData);
