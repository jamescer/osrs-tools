import { Item } from "../Item";

const MountedAlchemicalHydraHeadInterfaceItemData = {
  id: interface23081,
  name: "Mounted Alchemical Hydra head (interface item)"
};
export const MountedAlchemicalHydraHeadInterfaceItem = Item.fromJson(MountedAlchemicalHydraHeadInterfaceItemData);