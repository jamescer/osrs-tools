import { Item } from "../Item";

const MountedCoinsInterfaceItemData = {
  id: interface20631,
  name: "Mounted coins (interface item)"
};
export const MountedCoinsInterfaceItem = Item.fromJson(MountedCoinsInterfaceItemData);
