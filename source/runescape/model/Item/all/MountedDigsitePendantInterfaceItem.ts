import { Item } from "../Item";

const MountedDigsitePendantInterfaceItemData = {
  id: interface22709,
  name: "Mounted digsite pendant (interface item)"
};
export const MountedDigsitePendantInterfaceItem = Item.fromJson(MountedDigsitePendantInterfaceItemData);