import { Item } from "../Item";

const MountedEmblemInterfaceItemData = {
  id: interface20630,
  name: "Mounted emblem (interface item)"
};
export const MountedEmblemInterfaceItem = Item.fromJson(MountedEmblemInterfaceItemData);
