import { Item } from "../Item";

const MountedHarpoonfishInterfaceItemData = {
  id: interface11578,
  name: "Mounted harpoonfish (interface item)"
};
export const MountedHarpoonfishInterfaceItem = Item.fromJson(MountedHarpoonfishInterfaceItemData);