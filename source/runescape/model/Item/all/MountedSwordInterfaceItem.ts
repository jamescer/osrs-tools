import { Item } from "../Item";

const MountedSwordInterfaceItemData = {
  id: interface8090,
  name: "Mounted sword (interface item)"
};
export const MountedSwordInterfaceItem = Item.fromJson(MountedSwordInterfaceItemData);
