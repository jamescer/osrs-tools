import { Item } from "../Item";

const MountedXericsTalismanInterfaceItemData = {
  id: interface22708,
  name: "Mounted xeric's talisman (interface item)"
};
export const MountedXericsTalismanInterfaceItem = Item.fromJson(MountedXericsTalismanInterfaceItemData);