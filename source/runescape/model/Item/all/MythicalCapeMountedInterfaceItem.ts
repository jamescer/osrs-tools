import { Item } from "../Item";

const MythicalCapeMountedInterfaceItemData = {
  id: interface21913,
  name: "Mythical cape (mounted) (interface item)"
};
export const MythicalCapeMountedInterfaceItem = Item.fromJson(MythicalCapeMountedInterfaceItemData);
