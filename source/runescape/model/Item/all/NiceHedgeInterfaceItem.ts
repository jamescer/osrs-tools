import { Item } from "../Item";

const NiceHedgeInterfaceItemData = {
  id: interface8204,
  name: "Nice hedge (interface item)"
};
export const NiceHedgeInterfaceItem = Item.fromJson(NiceHedgeInterfaceItemData);
