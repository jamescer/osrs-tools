import { Item } from "../Item";

const NiceTreeInterfaceItemData = {
  id: interface8174,
  name: "Nice tree (interface item)"
};
export const NiceTreeInterfaceItem = Item.fromJson(NiceTreeInterfaceItemData);
