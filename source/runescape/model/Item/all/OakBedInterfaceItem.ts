import { Item } from "../Item";

const OakBedInterfaceItemData = {
  id: interface8032,
  name: "Oak bed (interface item)"
};
export const OakBedInterfaceItem = Item.fromJson(OakBedInterfaceItemData);