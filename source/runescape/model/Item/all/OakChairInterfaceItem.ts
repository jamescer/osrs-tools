import { Item } from "../Item";

const OakChairInterfaceItemData = {
  id: interface8312,
  name: "Oak chair (interface item)"
};
export const OakChairInterfaceItem = Item.fromJson(OakChairInterfaceItemData);
