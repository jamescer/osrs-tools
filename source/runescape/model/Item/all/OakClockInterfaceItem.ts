import { Item } from "../Item";

const OakClockInterfaceItemData = {
  id: interface8052,
  name: "Oak clock (interface item)"
};
export const OakClockInterfaceItem = Item.fromJson(OakClockInterfaceItemData);
