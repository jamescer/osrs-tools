import { Item } from "../Item";

const OakDiningTableInterfaceItemData = {
  id: interface8116,
  name: "Oak dining table (interface item)"
};
export const OakDiningTableInterfaceItem = Item.fromJson(OakDiningTableInterfaceItemData);