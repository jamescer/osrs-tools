import { Item } from "../Item";

const OakDoorInterfaceItemData = {
  id: interface8122,
  name: "Oak door (interface item)"
};
export const OakDoorInterfaceItem = Item.fromJson(OakDoorInterfaceItemData);
