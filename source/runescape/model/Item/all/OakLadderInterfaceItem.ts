import { Item } from "../Item";

const OakLadderInterfaceItemData = {
  id: interface8306,
  name: "Oak ladder (interface item)"
};
export const OakLadderInterfaceItem = Item.fromJson(OakLadderInterfaceItemData);
