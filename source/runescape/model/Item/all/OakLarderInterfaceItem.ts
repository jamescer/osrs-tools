import { Item } from "../Item";

const OakLarderInterfaceItemData = {
  id: interface8234,
  name: "Oak larder (interface item)"
};
export const OakLarderInterfaceItem = Item.fromJson(OakLarderInterfaceItemData);
