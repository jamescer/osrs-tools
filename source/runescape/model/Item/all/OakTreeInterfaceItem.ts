import { Item } from "../Item";

const OakTreeInterfaceItemData = {
  id: interface8175,
  name: "Oak tree (interface item)"
};
export const OakTreeInterfaceItem = Item.fromJson(OakTreeInterfaceItemData);
