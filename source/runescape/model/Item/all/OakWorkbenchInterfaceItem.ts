import { Item } from "../Item";

const OakWorkbenchInterfaceItemData = {
  id: interface8376,
  name: "Oak workbench (interface item)"
};
export const OakWorkbenchInterfaceItem = Item.fromJson(OakWorkbenchInterfaceItemData);
