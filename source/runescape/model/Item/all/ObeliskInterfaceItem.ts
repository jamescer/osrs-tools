import { Item } from "../Item";

const ObeliskInterfaceItemData = {
  id: interface21788,
  name: "Obelisk (interface item)"
};
export const ObeliskInterfaceItem = Item.fromJson(ObeliskInterfaceItemData);