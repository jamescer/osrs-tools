import { Item } from "../Item";

const OpulentRugInterfaceItemData = {
  id: interface25363,
  name: "Opulent rug (interface item)"
};
export const OpulentRugInterfaceItem = Item.fromJson(OpulentRugInterfaceItemData);