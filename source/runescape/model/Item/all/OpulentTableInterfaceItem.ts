import { Item } from "../Item";

const OpulentTableInterfaceItemData = {
  id: interface8121,
  name: "Opulent table (interface item)"
};
export const OpulentTableInterfaceItem = Item.fromJson(OpulentTableInterfaceItemData);