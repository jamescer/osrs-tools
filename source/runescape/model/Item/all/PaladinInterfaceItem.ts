import { Item } from "../Item";

const PaladinInterfaceItemData = {
  id: interface3255,
  name: "Paladin (interface item)"
};
export const PaladinInterfaceItem = Item.fromJson(PaladinInterfaceItemData);