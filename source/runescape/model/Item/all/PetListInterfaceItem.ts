import { Item } from "../Item";

const PetListInterfaceItemData = {
  id: interface12721,
  name: "Pet list (interface item)"
};
export const PetListInterfaceItem = Item.fromJson(PetListInterfaceItemData);