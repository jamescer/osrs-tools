import { Item } from "../Item";

const PicketFenceInterfaceItemData = {
  id: interface8200,
  name: "Picket fence (interface item)"
};
export const PicketFenceInterfaceItem = Item.fromJson(PicketFenceInterfaceItemData);