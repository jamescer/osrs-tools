import { Item } from "../Item";

const PlainSatchelInterfaceItemData = {
  id: interface25618,
  name: "Plain satchel (interface item)"
};
export const PlainSatchelInterfaceItem = Item.fromJson(PlainSatchelInterfaceItemData);