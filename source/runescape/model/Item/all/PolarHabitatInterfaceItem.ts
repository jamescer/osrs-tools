import { Item } from "../Item";

const PolarHabitatInterfaceItemData = {
  id: interface12713,
  name: "Polar habitat (interface item)"
};
export const PolarHabitatInterfaceItem = Item.fromJson(PolarHabitatInterfaceItemData);
