import { Item } from "../Item";

const PoshBellpullInterfaceItemData = {
  id: interface8101,
  name: "Posh bell-pull (interface item)"
};
export const PoshBellpullInterfaceItem = Item.fromJson(PoshBellpullInterfaceItemData);