import { Item } from "../Item";

const PoshFountainInterfaceItemData = {
  id: interface8195,
  name: "Posh fountain (interface item)"
};
export const PoshFountainInterfaceItem = Item.fromJson(PoshFountainInterfaceItemData);