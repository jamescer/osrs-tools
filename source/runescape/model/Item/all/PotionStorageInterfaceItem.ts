import { Item } from "../Item";

const PotionStorageInterfaceItemData = {
  id: interface29999,
  name: "Potion storage (interface item)"
};
export const PotionStorageInterfaceItem = Item.fromJson(PotionStorageInterfaceItemData);