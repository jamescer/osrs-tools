import { Item } from "../Item";

const PrayerInterfaceItemData = {
  id: interface3258,
  name: "Prayer (interface item)"
};
export const PrayerInterfaceItem = Item.fromJson(PrayerInterfaceItemData);
