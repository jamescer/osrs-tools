import { Item } from "../Item";

const PumpAndDrainInterfaceItemData = {
  id: interface8230,
  name: "Pump and drain (interface item)"
};
export const PumpAndDrainInterfaceItem = Item.fromJson(PumpAndDrainInterfaceItemData);
