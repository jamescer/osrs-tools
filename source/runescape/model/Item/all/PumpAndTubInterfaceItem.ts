import { Item } from "../Item";

const PumpAndTubInterfaceItemData = {
  id: interface8231,
  name: "Pump and tub (interface item)"
};
export const PumpAndTubInterfaceItem = Item.fromJson(PumpAndTubInterfaceItemData);
