import { Item } from "../Item";

const PyreFoxInterfaceItemData = {
  id: interface28865,
  name: "Pyre fox (interface item)"
};
export const PyreFoxInterfaceItem = Item.fromJson(PyreFoxInterfaceItemData);