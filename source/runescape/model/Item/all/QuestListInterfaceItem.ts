import { Item } from "../Item";

const QuestListInterfaceItemData = {
  id: interface20633,
  name: "Quest list (interface item)"
};
export const QuestListInterfaceItem = Item.fromJson(QuestListInterfaceItemData);