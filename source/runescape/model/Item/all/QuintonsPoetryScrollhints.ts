import { Item } from "../Item";

const QuintonsPoetryScrollhintsData = {
  id: hist28107,
  name: "Quinton's poetry scroll#Hints"
};
export const QuintonsPoetryScrollhints = Item.fromJson(QuintonsPoetryScrollhintsData);
