import { Item } from "../Item";

const QuintonsPoetryScrollunfinishedData = {
  id: hist28100,
  name: "Quinton's poetry scroll#Unfinished"
};
export const QuintonsPoetryScrollunfinished = Item.fromJson(QuintonsPoetryScrollunfinishedData);
