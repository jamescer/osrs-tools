import { Item } from "../Item";

const RangingPedestalsInterfaceItemData = {
  id: interface8026,
  name: "Ranging pedestals (interface item)"
};
export const RangingPedestalsInterfaceItem = Item.fromJson(RangingPedestalsInterfaceItemData);
