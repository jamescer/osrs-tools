import { Item } from "../Item";

const RedSatchelInterfaceItemData = {
  id: interface25620,
  name: "Red satchel (interface item)"
};
export const RedSatchelInterfaceItem = Item.fromJson(RedSatchelInterfaceItemData);