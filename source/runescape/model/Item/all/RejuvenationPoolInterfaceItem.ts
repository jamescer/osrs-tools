import { Item } from "../Item";

const RejuvenationPoolInterfaceItemData = {
  id: interface20641,
  name: "Rejuvenation pool (interface item)"
};
export const RejuvenationPoolInterfaceItem = Item.fromJson(RejuvenationPoolInterfaceItemData);
