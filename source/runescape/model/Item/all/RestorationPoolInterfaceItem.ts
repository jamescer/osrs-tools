import { Item } from "../Item";

const RestorationPoolInterfaceItemData = {
  id: interface20639,
  name: "Restoration pool (interface item)"
};
export const RestorationPoolInterfaceItem = Item.fromJson(RestorationPoolInterfaceItemData);