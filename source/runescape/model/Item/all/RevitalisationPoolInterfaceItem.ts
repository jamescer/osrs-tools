import { Item } from "../Item";

const RevitalisationPoolInterfaceItemData = {
  id: interface20640,
  name: "Revitalisation pool (interface item)"
};
export const RevitalisationPoolInterfaceItem = Item.fromJson(RevitalisationPoolInterfaceItemData);