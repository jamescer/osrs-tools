import { Item } from "../Item";

const RockingChairInterfaceItemData = {
  id: interface8311,
  name: "Rocking chair (interface item)"
};
export const RockingChairInterfaceItem = Item.fromJson(RockingChairInterfaceItemData);