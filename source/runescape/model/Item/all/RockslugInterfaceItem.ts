import { Item } from "../Item";

const RockslugInterfaceItemData = {
  id: interface4136,
  name: "Rockslug (interface item)"
};
export const RockslugInterfaceItem = Item.fromJson(RockslugInterfaceItemData);
