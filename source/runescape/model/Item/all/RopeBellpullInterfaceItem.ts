import { Item } from "../Item";

const RopeBellpullInterfaceItemData = {
  id: interface8099,
  name: "Rope bell-pull (interface item)"
};
export const RopeBellpullInterfaceItem = Item.fromJson(RopeBellpullInterfaceItemData);
