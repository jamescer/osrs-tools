import { Item } from "../Item";

const RosemaryInterfaceItemData = {
  id: interface8210,
  name: "Rosemary (interface item)"
};
export const RosemaryInterfaceItem = Item.fromJson(RosemaryInterfaceItemData);
