import { Item } from "../Item";

const RoundShieldInterfaceItemData = {
  id: interface8105,
  name: "Round shield (interface item)"
};
export const RoundShieldInterfaceItem = Item.fromJson(RoundShieldInterfaceItemData);
