import { Item } from "../Item";

const RubyHarvestInterfaceItemData = {
  id: interface9970,
  name: "Ruby harvest (interface item)"
};
export const RubyHarvestInterfaceItem = Item.fromJson(RubyHarvestInterfaceItemData);
