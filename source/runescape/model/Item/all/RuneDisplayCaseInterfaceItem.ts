import { Item } from "../Item";

const RuneDisplayCaseInterfaceItemData = {
  id: interface8095,
  name: "Rune display case (interface item)"
};
export const RuneDisplayCaseInterfaceItem = Item.fromJson(RuneDisplayCaseInterfaceItemData);
