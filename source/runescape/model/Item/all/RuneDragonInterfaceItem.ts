import { Item } from "../Item";

const RuneDragonInterfaceItemData = {
  id: interface21911,
  name: "Rune dragon (interface item)"
};
export const RuneDragonInterfaceItem = Item.fromJson(RuneDragonInterfaceItemData);
