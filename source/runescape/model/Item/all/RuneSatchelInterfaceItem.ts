import { Item } from "../Item";

const RuneSatchelInterfaceItemData = {
  id: interface25623,
  name: "Rune satchel (interface item)"
};
export const RuneSatchelInterfaceItem = Item.fromJson(RuneSatchelInterfaceItemData);
