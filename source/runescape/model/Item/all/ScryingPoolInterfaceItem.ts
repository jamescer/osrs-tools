import { Item } from "../Item";

const ScryingPoolInterfaceItemData = {
  id: interface8333,
  name: "Scrying pool (interface item)"
};
export const ScryingPoolInterfaceItem = Item.fromJson(ScryingPoolInterfaceItemData);