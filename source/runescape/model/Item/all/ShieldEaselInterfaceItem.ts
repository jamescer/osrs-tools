import { Item } from "../Item";

const ShieldEaselInterfaceItemData = {
  id: interface8393,
  name: "Shield easel (interface item)"
};
export const ShieldEaselInterfaceItem = Item.fromJson(ShieldEaselInterfaceItemData);