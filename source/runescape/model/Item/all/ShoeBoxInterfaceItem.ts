import { Item } from "../Item";

const ShoeBoxInterfaceItemData = {
  id: interface8038,
  name: "Shoe box (interface item)"
};
export const ShoeBoxInterfaceItem = Item.fromJson(ShoeBoxInterfaceItemData);
