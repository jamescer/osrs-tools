import { Item } from "../Item";

const ShutteredWindowInterfaceItemData = {
  id: interface8076,
  name: "Shuttered window (interface item)"
};
export const ShutteredWindowInterfaceItem = Item.fromJson(ShutteredWindowInterfaceItemData);