import { Item } from "../Item";

const SimpleArenaInterfaceItemData = {
  id: interface12718,
  name: "Simple arena (interface item)"
};
export const SimpleArenaInterfaceItem = Item.fromJson(SimpleArenaInterfaceItemData);
