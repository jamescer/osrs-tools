import { Item } from "../Item";

const SkeletonGuardInterfaceItemData = {
  id: interface8131,
  name: "Skeleton guard (interface item)"
};
export const SkeletonGuardInterfaceItem = Item.fromJson(SkeletonGuardInterfaceItemData);
