import { Item } from "../Item";

const SmallBoxHedgeInterfaceItemData = {
  id: interface8205,
  name: "Small box hedge (interface item)"
};
export const SmallBoxHedgeInterfaceItem = Item.fromJson(SmallBoxHedgeInterfaceItemData);