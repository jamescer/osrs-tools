import { Item } from "../Item";

const SmallFountainInterfaceItemData = {
  id: interface8193,
  name: "Small fountain (interface item)"
};
export const SmallFountainInterfaceItem = Item.fromJson(SmallFountainInterfaceItemData);