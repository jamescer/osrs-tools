import { Item } from "../Item";

const SmallLandscapeInterfaceItemData = {
  id: interface8091,
  name: "Small landscape (interface item)"
};
export const SmallLandscapeInterfaceItem = Item.fromJson(SmallLandscapeInterfaceItemData);
