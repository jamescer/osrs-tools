import { Item } from "../Item";

const SmallMapInterfaceItemData = {
  id: interface8294,
  name: "Small map (interface item)"
};
export const SmallMapInterfaceItem = Item.fromJson(SmallMapInterfaceItemData);
