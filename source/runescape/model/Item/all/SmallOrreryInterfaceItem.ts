import { Item } from "../Item";

const SmallOrreryInterfaceItemData = {
  id: interface8346,
  name: "Small orrery (interface item)"
};
export const SmallOrreryInterfaceItem = Item.fromJson(SmallOrreryInterfaceItemData);
