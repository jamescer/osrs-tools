import { Item } from "../Item";

const SmallOvenInterfaceItemData = {
  id: interface8219,
  name: "Small oven (interface item)"
};
export const SmallOvenInterfaceItem = Item.fromJson(SmallOvenInterfaceItemData);