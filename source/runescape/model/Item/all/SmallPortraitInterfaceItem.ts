import { Item } from "../Item";

const SmallPortraitInterfaceItemData = {
  id: interface8086,
  name: "Small portrait (interface item)"
};
export const SmallPortraitInterfaceItem = Item.fromJson(SmallPortraitInterfaceItemData);
