import { Item } from "../Item";

const SmallStatueInterfaceItemData = {
  id: interface8082,
  name: "Small statue (interface item)"
};
export const SmallStatueInterfaceItem = Item.fromJson(SmallStatueInterfaceItemData);