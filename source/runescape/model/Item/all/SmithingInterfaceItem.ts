import { Item } from "../Item";

const SmithingInterfaceItemData = {
  id: interface5086,
  name: "Smithing (interface item)"
};
export const SmithingInterfaceItem = Item.fromJson(SmithingInterfaceItemData);