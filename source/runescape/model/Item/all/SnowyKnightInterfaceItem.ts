import { Item } from "../Item";

const SnowyKnightInterfaceItemData = {
  id: interface9972,
  name: "Snowy knight (interface item)"
};
export const SnowyKnightInterfaceItem = Item.fromJson(SnowyKnightInterfaceItemData);
