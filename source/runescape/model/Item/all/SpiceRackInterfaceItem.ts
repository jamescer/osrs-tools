import { Item } from "../Item";

const SpiceRackInterfaceItemData = {
  id: interface24479,
  name: "Spice rack (interface item)"
};
export const SpiceRackInterfaceItem = Item.fromJson(SpiceRackInterfaceItemData);