import { Item } from "../Item";

const SpikeTrapInterfaceItemData = {
  id: interface8143,
  name: "Spike trap (interface item)"
};
export const SpikeTrapInterfaceItem = Item.fromJson(SpikeTrapInterfaceItemData);