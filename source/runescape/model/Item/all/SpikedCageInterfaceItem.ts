import { Item } from "../Item";

const SpikedCageInterfaceItemData = {
  id: interface8300,
  name: "Spiked cage (interface item)"
};
export const SpikedCageInterfaceItem = Item.fromJson(SpikedCageInterfaceItemData);