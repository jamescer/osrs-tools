import { Item } from "../Item";

const SpikesInterfaceItemData = {
  id: interface8302,
  name: "Spikes (interface item)"
};
export const SpikesInterfaceItem = Item.fromJson(SpikesInterfaceItemData);
