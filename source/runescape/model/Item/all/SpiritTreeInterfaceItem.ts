import { Item } from "../Item";

const SpiritTreeInterfaceItemData = {
  id: interface20635,
  name: "Spirit tree (interface item)"
};
export const SpiritTreeInterfaceItem = Item.fromJson(SpiritTreeInterfaceItemData);