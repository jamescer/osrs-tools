import { Item } from "../Item";

const SpiritualMageInterfaceItemData = {
  id: interface11844,
  name: "Spiritual Mage (interface item)"
};
export const SpiritualMageInterfaceItem = Item.fromJson(SpiritualMageInterfaceItemData);