import { Item } from "../Item";

const SpiritualRangerInterfaceItemData = {
  id: interface11845,
  name: "Spiritual Ranger (interface item)"
};
export const SpiritualRangerInterfaceItem = Item.fromJson(SpiritualRangerInterfaceItemData);