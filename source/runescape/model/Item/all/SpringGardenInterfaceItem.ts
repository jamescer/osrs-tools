import { Item } from "../Item";

const SpringGardenInterfaceItemData = {
  id: interface10853,
  name: "Spring garden (interface item)"
};
export const SpringGardenInterfaceItem = Item.fromJson(SpringGardenInterfaceItemData);
