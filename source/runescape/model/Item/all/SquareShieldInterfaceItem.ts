import { Item } from "../Item";

const SquareShieldInterfaceItemData = {
  id: interface8106,
  name: "Square shield (interface item)"
};
export const SquareShieldInterfaceItem = Item.fromJson(SquareShieldInterfaceItemData);
