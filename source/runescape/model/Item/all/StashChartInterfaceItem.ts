import { Item } from "../Item";

const StashChartInterfaceItemData = {
  id: interface25611,
  name: "S.t.a.s.h chart (interface item)"
};
export const StashChartInterfaceItem = Item.fromJson(StashChartInterfaceItemData);
