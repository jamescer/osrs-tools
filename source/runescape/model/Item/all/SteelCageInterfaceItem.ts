import { Item } from "../Item";

const SteelCageInterfaceItemData = {
  id: interface8371,
  name: "Steel cage (interface item)"
};
export const SteelCageInterfaceItem = Item.fromJson(SteelCageInterfaceItemData);