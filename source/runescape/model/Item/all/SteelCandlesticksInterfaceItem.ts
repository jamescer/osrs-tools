import { Item } from "../Item";

const SteelCandlesticksInterfaceItemData = {
  id: interface8071,
  name: "Steel candlesticks (interface item)"
};
export const SteelCandlesticksInterfaceItem = Item.fromJson(SteelCandlesticksInterfaceItemData);