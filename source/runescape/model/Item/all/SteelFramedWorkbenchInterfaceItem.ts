import { Item } from "../Item";

const SteelFramedWorkbenchInterfaceItemData = {
  id: interface8377,
  name: "Steel framed workbench (interface item)"
};
export const SteelFramedWorkbenchInterfaceItem = Item.fromJson(SteelFramedWorkbenchInterfaceItemData);
