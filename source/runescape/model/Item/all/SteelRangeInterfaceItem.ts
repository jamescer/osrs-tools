import { Item } from "../Item";

const SteelRangeInterfaceItemData = {
  id: interface8221,
  name: "Steel range (interface item)"
};
export const SteelRangeInterfaceItem = Item.fromJson(SteelRangeInterfaceItemData);
