import { Item } from "../Item";

const SteelplatedDoorInterfaceItemData = {
  id: interface8123,
  name: "Steel-plated door (interface item)"
};
export const SteelplatedDoorInterfaceItem = Item.fromJson(SteelplatedDoorInterfaceItemData);
