import { Item } from "../Item";

const StoneFireplaceInterfaceItemData = {
  id: interface8326,
  name: "Stone fireplace (interface item)"
};
export const StoneFireplaceInterfaceItem = Item.fromJson(StoneFireplaceInterfaceItemData);