import { Item } from "../Item";

const StoneWallInterfaceItemData = {
  id: interface8198,
  name: "Stone wall (interface item)"
};
export const StoneWallInterfaceItem = Item.fromJson(StoneWallInterfaceItemData);
