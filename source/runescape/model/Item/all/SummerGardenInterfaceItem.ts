import { Item } from "../Item";

const SummerGardenInterfaceItemData = {
  id: interface10852,
  name: "Summer garden (interface item)"
};
export const SummerGardenInterfaceItem = Item.fromJson(SummerGardenInterfaceItemData);
