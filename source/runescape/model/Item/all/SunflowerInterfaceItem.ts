import { Item } from "../Item";

const SunflowerInterfaceItemData = {
  id: interface8213,
  name: "Sunflower (interface item)"
};
export const SunflowerInterfaceItem = Item.fromJson(SunflowerInterfaceItemData);
