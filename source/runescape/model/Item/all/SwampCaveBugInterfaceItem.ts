import { Item } from "../Item";

const SwampCaveBugInterfaceItemData = {
  id: interface4521,
  name: "Swamp cave bug (interface item)"
};
export const SwampCaveBugInterfaceItem = Item.fromJson(SwampCaveBugInterfaceItemData);
