import { Item } from "../Item";

const TabbithasPoetryScrollunfinishedData = {
  id: hist28103,
  name: "Tabbitha's poetry scroll#Unfinished"
};
export const TabbithasPoetryScrollunfinished = Item.fromJson(TabbithasPoetryScrollunfinishedData);
