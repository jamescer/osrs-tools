import { Item } from "../Item";

const TeakAltarInterfaceItemData = {
  id: interface8063,
  name: "Teak altar (interface item)"
};
export const TeakAltarInterfaceItem = Item.fromJson(TeakAltarInterfaceItemData);