import { Item } from "../Item";

const TeakBedInterfaceItemData = {
  id: interface8034,
  name: "Teak bed (interface item)"
};
export const TeakBedInterfaceItem = Item.fromJson(TeakBedInterfaceItemData);
