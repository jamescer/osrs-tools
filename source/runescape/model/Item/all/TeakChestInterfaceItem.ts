import { Item } from "../Item";

const TeakChestInterfaceItemData = {
  id: interface8150,
  name: "Teak chest (interface item)"
};
export const TeakChestInterfaceItem = Item.fromJson(TeakChestInterfaceItemData);