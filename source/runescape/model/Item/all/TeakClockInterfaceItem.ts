import { Item } from "../Item";

const TeakClockInterfaceItemData = {
  id: interface8053,
  name: "Teak clock (interface item)"
};
export const TeakClockInterfaceItem = Item.fromJson(TeakClockInterfaceItemData);