import { Item } from "../Item";

const TeakDiningBenchInterfaceItemData = {
  id: interface8111,
  name: "Teak dining bench (interface item)"
};
export const TeakDiningBenchInterfaceItem = Item.fromJson(TeakDiningBenchInterfaceItemData);
