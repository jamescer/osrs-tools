import { Item } from "../Item";

const TeakDisplayInterfaceItemData = {
  id: interface11603,
  name: "Teak display (interface item)"
};
export const TeakDisplayInterfaceItem = Item.fromJson(TeakDisplayInterfaceItemData);