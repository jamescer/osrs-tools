import { Item } from "../Item";

const TeakDrawersInterfaceItemData = {
  id: interface8041,
  name: "Teak drawers (interface item)"
};
export const TeakDrawersInterfaceItem = Item.fromJson(TeakDrawersInterfaceItemData);