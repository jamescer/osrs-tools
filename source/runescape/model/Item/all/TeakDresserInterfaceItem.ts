import { Item } from "../Item";

const TeakDresserInterfaceItemData = {
  id: interface8048,
  name: "Teak dresser (interface item)"
};
export const TeakDresserInterfaceItem = Item.fromJson(TeakDresserInterfaceItemData);