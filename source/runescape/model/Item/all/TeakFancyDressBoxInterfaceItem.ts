import { Item } from "../Item";

const TeakFancyDressBoxInterfaceItemData = {
  id: interface9824,
  name: "Teak fancy dress box (interface item)"
};
export const TeakFancyDressBoxInterfaceItem = Item.fromJson(TeakFancyDressBoxInterfaceItemData);
