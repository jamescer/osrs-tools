import { Item } from "../Item";

const TeakFeederInterfaceItemData = {
  id: interface12723,
  name: "Teak feeder (interface item)"
};
export const TeakFeederInterfaceItem = Item.fromJson(TeakFeederInterfaceItemData);
