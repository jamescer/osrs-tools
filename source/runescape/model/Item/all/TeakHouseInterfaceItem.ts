import { Item } from "../Item";

const TeakHouseInterfaceItemData = {
  id: interface12705,
  name: "Teak house (interface item)"
};
export const TeakHouseInterfaceItem = Item.fromJson(TeakHouseInterfaceItemData);