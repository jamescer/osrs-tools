import { Item } from "../Item";

const TeakKitchenTableInterfaceItemData = {
  id: interface8248,
  name: "Teak kitchen table (interface item)"
};
export const TeakKitchenTableInterfaceItem = Item.fromJson(TeakKitchenTableInterfaceItemData);
