import { Item } from "../Item";

const TeakLadderInterfaceItemData = {
  id: interface8307,
  name: "Teak ladder (interface item)"
};
export const TeakLadderInterfaceItem = Item.fromJson(TeakLadderInterfaceItemData);