import { Item } from "../Item";

const TeakLarderInterfaceItemData = {
  id: interface8235,
  name: "Teak larder (interface item)"
};
export const TeakLarderInterfaceItem = Item.fromJson(TeakLarderInterfaceItemData);
