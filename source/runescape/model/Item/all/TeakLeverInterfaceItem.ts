import { Item } from "../Item";

const TeakLeverInterfaceItemData = {
  id: interface8365,
  name: "Teak lever (interface item)"
};
export const TeakLeverInterfaceItem = Item.fromJson(TeakLeverInterfaceItemData);
