import { Item } from "../Item";

const TeakMountedHeadDisplayInterfaceItemData = {
  id: interface8087,
  name: "Teak mounted head display (interface item)"
};
export const TeakMountedHeadDisplayInterfaceItem = Item.fromJson(TeakMountedHeadDisplayInterfaceItemData);
