import { Item } from "../Item";

const TeakPortalInterfaceItemData = {
  id: interface8328,
  name: "Teak portal (interface item)"
};
export const TeakPortalInterfaceItem = Item.fromJson(TeakPortalInterfaceItemData);
