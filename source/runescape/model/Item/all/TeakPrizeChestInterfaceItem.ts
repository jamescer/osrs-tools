import { Item } from "../Item";

const TeakPrizeChestInterfaceItemData = {
  id: interface8166,
  name: "Teak prize chest (interface item)"
};
export const TeakPrizeChestInterfaceItem = Item.fromJson(TeakPrizeChestInterfaceItemData);