import { Item } from "../Item";

const TeakShelves1InterfaceItemData = {
  id: interface8228,
  name: "Teak shelves 1 (interface item)"
};
export const TeakShelves1InterfaceItem = Item.fromJson(TeakShelves1InterfaceItemData);
