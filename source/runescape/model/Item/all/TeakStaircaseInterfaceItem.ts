import { Item } from "../Item";

const TeakStaircaseInterfaceItemData = {
  id: interface8252,
  name: "Teak staircase (interface item)"
};
export const TeakStaircaseInterfaceItem = Item.fromJson(TeakStaircaseInterfaceItemData);
