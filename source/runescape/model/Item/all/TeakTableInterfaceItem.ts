import { Item } from "../Item";

const TeakTableInterfaceItemData = {
  id: interface8118,
  name: "Teak table (interface item)"
};
export const TeakTableInterfaceItem = Item.fromJson(TeakTableInterfaceItemData);