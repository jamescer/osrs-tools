import { Item } from "../Item";

const TeleportFocusInterfaceItemData = {
  id: interface8331,
  name: "Teleport focus (interface item)"
};
export const TeleportFocusInterfaceItem = Item.fromJson(TeleportFocusInterfaceItemData);
