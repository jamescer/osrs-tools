import { Item } from "../Item";

const TeleportTrapInterfaceItemData = {
  id: interface8147,
  name: "Teleport trap (interface item)"
};
export const TeleportTrapInterfaceItem = Item.fromJson(TeleportTrapInterfaceItemData);