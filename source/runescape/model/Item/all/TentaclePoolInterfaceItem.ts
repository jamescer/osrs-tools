import { Item } from "../Item";

const TentaclePoolInterfaceItemData = {
  id: interface8303,
  name: "Tentacle pool (interface item)"
};
export const TentaclePoolInterfaceItem = Item.fromJson(TentaclePoolInterfaceItemData);
