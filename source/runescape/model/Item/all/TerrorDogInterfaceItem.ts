import { Item } from "../Item";

const TerrorDogInterfaceItemData = {
  id: interface10591,
  name: "Terror dog (interface item)"
};
export const TerrorDogInterfaceItem = Item.fromJson(TerrorDogInterfaceItemData);