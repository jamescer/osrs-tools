import { Item } from "../Item";

const ThornyHedgeInterfaceItemData = {
  id: interface8203,
  name: "Thorny hedge (interface item)"
};
export const ThornyHedgeInterfaceItem = Item.fromJson(ThornyHedgeInterfaceItemData);
