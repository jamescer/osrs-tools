import { Item } from "../Item";

const TipJarInterfaceItemData = {
  id: interface20634,
  name: "Tip jar (interface item)"
};
export const TipJarInterfaceItem = Item.fromJson(TipJarInterfaceItemData);
