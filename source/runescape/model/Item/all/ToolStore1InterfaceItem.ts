import { Item } from "../Item";

const ToolStore1InterfaceItemData = {
  id: interface8384,
  name: "Tool store 1 (interface item)"
};
export const ToolStore1InterfaceItem = Item.fromJson(ToolStore1InterfaceItemData);
