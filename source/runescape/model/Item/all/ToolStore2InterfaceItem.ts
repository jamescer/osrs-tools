import { Item } from "../Item";

const ToolStore2InterfaceItemData = {
  id: interface8385,
  name: "Tool store 2 (interface item)"
};
export const ToolStore2InterfaceItem = Item.fromJson(ToolStore2InterfaceItemData);
