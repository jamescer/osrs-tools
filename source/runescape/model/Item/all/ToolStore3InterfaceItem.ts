import { Item } from "../Item";

const ToolStore3InterfaceItemData = {
  id: interface8386,
  name: "Tool store 3 (interface item)"
};
export const ToolStore3InterfaceItem = Item.fromJson(ToolStore3InterfaceItemData);