import { Item } from "../Item";

const ToolStore4InterfaceItemData = {
  id: interface8387,
  name: "Tool store 4 (interface item)"
};
export const ToolStore4InterfaceItem = Item.fromJson(ToolStore4InterfaceItemData);