import { Item } from "../Item";

const ToolStore5InterfaceItemData = {
  id: interface8388,
  name: "Tool store 5 (interface item)"
};
export const ToolStore5InterfaceItem = Item.fromJson(ToolStore5InterfaceItemData);