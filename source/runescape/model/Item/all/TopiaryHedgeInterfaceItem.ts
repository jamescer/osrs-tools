import { Item } from "../Item";

const TopiaryHedgeInterfaceItemData = {
  id: interface8206,
  name: "Topiary hedge (interface item)"
};
export const TopiaryHedgeInterfaceItem = Item.fromJson(TopiaryHedgeInterfaceItemData);