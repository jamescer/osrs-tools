import { Item } from "../Item";

const ToxicBlowpipeBetablackData = {
  id: beta25487,
  name: "Toxic blowpipe (beta)#Black"
};
export const ToxicBlowpipeBetablack = Item.fromJson(ToxicBlowpipeBetablackData);
