import { Item } from "../Item";

const ToxicBlowpipeBetabronzeData = {
  id: beta25484,
  name: "Toxic blowpipe (beta)#Bronze"
};
export const ToxicBlowpipeBetabronze = Item.fromJson(ToxicBlowpipeBetabronzeData);