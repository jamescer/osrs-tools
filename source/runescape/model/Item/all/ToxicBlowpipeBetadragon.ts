import { Item } from "../Item";

const ToxicBlowpipeBetadragonData = {
  id: beta25491,
  name: "Toxic blowpipe (beta)#Dragon"
};
export const ToxicBlowpipeBetadragon = Item.fromJson(ToxicBlowpipeBetadragonData);