import { Item } from "../Item";

const ToxicBlowpipeBetamithrilData = {
  id: beta25488,
  name: "Toxic blowpipe (beta)#Mithril"
};
export const ToxicBlowpipeBetamithril = Item.fromJson(ToxicBlowpipeBetamithrilData);
