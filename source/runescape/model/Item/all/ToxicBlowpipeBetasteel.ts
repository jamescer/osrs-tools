import { Item } from "../Item";

const ToxicBlowpipeBetasteelData = {
  id: beta25486,
  name: "Toxic blowpipe (beta)#Steel"
};
export const ToxicBlowpipeBetasteel = Item.fromJson(ToxicBlowpipeBetasteelData);