import { Item } from "../Item";

const TrailblazerRugInterfaceItemData = {
  id: interface25364,
  name: "Trailblazer rug (interface item)"
};
export const TrailblazerRugInterfaceItem = Item.fromJson(TrailblazerRugInterfaceItemData);
