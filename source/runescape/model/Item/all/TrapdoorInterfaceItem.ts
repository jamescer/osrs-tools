import { Item } from "../Item";

const TrapdoorInterfaceItemData = {
  id: interface8372,
  name: "Trapdoor (interface item)"
};
export const TrapdoorInterfaceItem = Item.fromJson(TrapdoorInterfaceItemData);
