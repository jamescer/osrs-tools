import { Item } from "../Item";

const TreasureHuntInterfaceItemData = {
  id: interface8160,
  name: "Treasure hunt (interface item)"
};
export const TreasureHuntInterfaceItem = Item.fromJson(TreasureHuntInterfaceItemData);
