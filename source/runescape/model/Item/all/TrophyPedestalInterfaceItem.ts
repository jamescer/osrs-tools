import { Item } from "../Item";

const TrophyPedestalInterfaceItemData = {
  id: interface25351,
  name: "Trophy pedestal (interface item)"
};
export const TrophyPedestalInterfaceItem = Item.fromJson(TrophyPedestalInterfaceItemData);
