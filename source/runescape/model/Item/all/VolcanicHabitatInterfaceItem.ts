import { Item } from "../Item";

const VolcanicHabitatInterfaceItemData = {
  id: interface12714,
  name: "Volcanic habitat (interface item)"
};
export const VolcanicHabitatInterfaceItem = Item.fromJson(VolcanicHabitatInterfaceItemData);