import { Item } from "../Item";

const WasteDisposalbaguetteData = {
  id: hist6969,
  name: "Waste disposal#Baguette"
};
export const WasteDisposalbaguette = Item.fromJson(WasteDisposalbaguetteData);