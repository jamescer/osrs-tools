import { Item } from "../Item";

const WatchmanInterfaceItemData = {
  id: interface3253,
  name: "Watchman (interface item)"
};
export const WatchmanInterfaceItem = Item.fromJson(WatchmanInterfaceItemData);
