import { Item } from "../Item";

const WeaponsRackInterfaceItemData = {
  id: interface8029,
  name: "Weapons rack (interface item)"
};
export const WeaponsRackInterfaceItem = Item.fromJson(WeaponsRackInterfaceItemData);