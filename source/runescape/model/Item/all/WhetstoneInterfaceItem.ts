import { Item } from "../Item";

const WhetstoneInterfaceItemData = {
  id: interface8390,
  name: "Whetstone (interface item)"
};
export const WhetstoneInterfaceItem = Item.fromJson(WhetstoneInterfaceItemData);