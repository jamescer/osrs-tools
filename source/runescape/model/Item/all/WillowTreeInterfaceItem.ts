import { Item } from "../Item";

const WillowTreeInterfaceItemData = {
  id: interface8176,
  name: "Willow tree (interface item)"
};
export const WillowTreeInterfaceItem = Item.fromJson(WillowTreeInterfaceItemData);