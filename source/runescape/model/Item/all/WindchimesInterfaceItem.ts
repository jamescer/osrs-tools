import { Item } from "../Item";

const WindchimesInterfaceItemData = {
  id: interface8079,
  name: "Windchimes (interface item)"
};
export const WindchimesInterfaceItem = Item.fromJson(WindchimesInterfaceItemData);
