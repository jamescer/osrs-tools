import { Item } from "../Item";

const WinterGardenInterfaceItemData = {
  id: interface10855,
  name: "Winter garden (interface item)"
};
export const WinterGardenInterfaceItem = Item.fromJson(WinterGardenInterfaceItemData);
