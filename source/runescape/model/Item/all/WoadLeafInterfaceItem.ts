import { Item } from "../Item";

const WoadLeafInterfaceItemData = {
  id: interface5738,
  name: "Woad leaf (interface item)"
};
export const WoadLeafInterfaceItem = Item.fromJson(WoadLeafInterfaceItemData);
