import { Item } from "../Item";

const WoodenFenceInterfaceItemData = {
  id: interface8197,
  name: "Wooden fence (interface item)"
};
export const WoodenFenceInterfaceItem = Item.fromJson(WoodenFenceInterfaceItemData);